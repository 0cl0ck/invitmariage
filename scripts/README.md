# scripts

Scripts utilitaires (à écrire **au moment du build**, pas maintenant).

Prévus :
- `optimize-video.*` — compression de la vidéo master en version web (résolution/bitrate, keyframes fréquentes pour le scrubbing).
- `extract-frames.*` — extraction d'une séquence d'images (si scrub sur `<canvas>` retenu).

Vide pour l'instant.
